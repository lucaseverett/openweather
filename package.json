{
  "name": "openweather",
  "scripts": {
    "start": "nodemon -r dotenv/config server.js",
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest --setupFiles dotenv/config",
    "test:coverage": "NODE_OPTIONS='--experimental-vm-modules' jest --coverage --setupFiles dotenv/config"
  },
  "type": "module",
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "helmet": "^5.1.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "supertest": "^6.2.4"
  }
}
